---
title: ActivitySim Outputs
date: today
---

```{python}
import toml
from pathlib import Path

config = toml.load(Path.cwd()/ 'validation_scripts/validation_configuration.toml')

# Relative path between notebooks and goruped output directories
output_path = Path(config["p_model_path"])

print(f"Run location: {output_path}")
```